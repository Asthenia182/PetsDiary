<UserControl x:Class="PetsDiary.Presentation.Views.VisitsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             xmlns:res="clr-namespace:PetsDiary.Presentation.Resources"
             xmlns:controls="clr-namespace:PetsDiary.Presentation.Controls"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>

        <CollectionViewSource x:Key="TimelineItems" Source="{Binding Visits}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Date" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>

        <DataTemplate x:Key="itemtp">
            <Border BorderBrush="{StaticResource GreenSheen}" BorderThickness="2,0,0,0" Padding="10,0,0,0">
                
                    <Border BorderThickness="1" Margin="0,5,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" CornerRadius="3" BorderBrush="{StaticResource RussianGreen}">
                    <DockPanel>
                        <controls:IconButton VerticalAlignment="Center" HorizontalAlignment="Right" DockPanel.Dock="Right" 
                                     IconHeight="10" IconWidth="10"
                                     IconSource="{StaticResource closeIcon}" 
                                     Style="{StaticResource IconButtonTemplate}"
                                     Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.DeleteCommand}" CommandParameter="{Binding Id}"/>
                        <controls:IconButton VerticalAlignment="Center" HorizontalAlignment="Right"  DockPanel.Dock="Right" Margin="5,0,0,0"
                                     IconHeight="15" IconWidth="15"
                                     IconSource="{StaticResource editIcon}" 
                                     Style="{StaticResource IconButtonTemplate}"
                                     Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.EditCommand}" CommandParameter="{Binding Id}"/>
                        <TextBlock DockPanel.Dock="Left" Text="{Binding Path=Description}" Width="300" FontSize="12" Margin="10"/>
                    </DockPanel>
                </Border>

            </Border>
        </DataTemplate>


    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <controls:IconButton VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,20,20,0"
                                     IconHeight="30" IconWidth="30"
                                     IconSource="{StaticResource plusIcon}" 
                                     Style="{StaticResource IconButtonTemplate}"
                                     Command="{Binding AddCommand}"/>
            <ItemsControl Grid.Row="1" ItemTemplate="{StaticResource itemtp}" HorizontalAlignment="Center" ItemsSource="{Binding Source={StaticResource TimelineItems}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical"></WrapPanel>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Grid.Row="0">
                                    <Ellipse Height="8" Width="8" Fill="{StaticResource GreenSheen}"/>
                                    <TextBlock Margin="6,5,0,5" Text="{Binding Name, StringFormat=dd MMMM yyyy}" FontSize="14" FontWeight="SemiBold"/>
                                </StackPanel>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                    </GroupStyle>
                </ItemsControl.GroupStyle>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
